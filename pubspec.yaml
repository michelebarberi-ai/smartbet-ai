name: smartbet_ai_new
description: "SmartBet AI"
publish_to: 'none'

version: 1.0.0+7

environment:
  sdk: ^3.12.2

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.1
  cupertino_icons: ^1.0.8
  flutter_dotenv: ^5.2.1
  shared_preferences: ^2.5.5
  share_plus: 12.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true

  assets:
    - .env
    - assets/smartbet_app_icon.png
